# Инструкция по развертыванию RP LAVKA

## 1. Настройка Supabase

### Создание проекта
1. Зайдите на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Запомните:
   - `Project URL` (например: https://xxx.supabase.co)
   - `anon public` ключ

### Создание таблиц
1. Откройте SQL Editor в Supabase
2. Скопируйте содержимое файла `supabase_setup.sql`
3. Выполните SQL запрос
4. Проверьте что таблицы созданы в разделе Table Editor

## 2. Настройка GitHub Pages

### Подготовка репозитория
1. Создайте репозиторий на GitHub (например: `rplavka`)
2. Скопируйте файлы из папки `web/` в корень репозитория:
   ```
   rplavka/
   ├── index.html
   ├── style.css
   └── app.js
   ```

### Настройка Supabase в коде
1. Откройте `app.js`
2. Замените:
   ```javascript
   const SUPABASE_URL = 'YOUR_SUPABASE_URL';
   const SUPABASE_KEY = 'YOUR_SUPABASE_KEY';
   ```
   На ваши данные из Supabase

### Включение GitHub Pages
1. Зайдите в Settings → Pages
2. Source: Deploy from a branch
3. Branch: main → / (root)
4. Сохраните

Ваш сайт будет доступен по адресу:
`https://yourusername.github.io/rplavka`

## 3. Настройка Telegram бота

### Создание бота
1. Напишите [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Сохраните токен бота

### Настройка Mini App
1. Откройте `bot.py`
2. Замените `MINI_APP_URL` на ваш GitHub Pages URL:
   ```python
   MINI_APP_URL = "https://yourusername.github.io/rplavka"
   ```

### Настройка Menu Button
1. Напишите [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Bot Settings → Menu Button
5. Укажите:
   - Button text: `🌴 RP LAVKA`
   - URL: `https://yourusername.github.io/rplavka`

## 4. Запуск бота

### Локально (для тестирования)
```bash
cd rplavka
pip install -r requirements.txt
python bot.py
```

### На сервере (постоянная работа)

#### Вариант 1: VPS/Dedicated сервер
```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск в фоне с помощью screen
screen -S rplavka
python bot.py
# Нажмите Ctrl+A, затем D для выхода

# Для возврата к боту
screen -r rplavka
```

#### Вариант 2: Heroku
1. Создайте `Procfile`:
   ```
   worker: python bot.py
   ```
2. Создайте `runtime.txt`:
   ```
   python-3.11.0
   ```
3. Деплой:
   ```bash
   heroku create rplavka-bot
   git push heroku main
   heroku ps:scale worker=1
   ```

#### Вариант 3: Railway.app
1. Зайдите на [railway.app](https://railway.app)
2. New Project → Deploy from GitHub
3. Выберите репозиторий
4. Добавьте переменные окружения:
   - `BOT_TOKEN` = ваш токен
5. Deploy

## 5. Проверка работы

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Нажмите кнопку "🌴 Открыть RP LAVKA"
4. Должен открыться Mini App с зелёным дизайном

## 6. Дополнительные настройки

### Добавление изображения машины
1. Найдите изображение Porsche в зелёных тонах
2. Сохраните как `car.png`
3. Загрузите в репозиторий рядом с `index.html`

### Настройка домена (опционально)
1. Купите домен (например: rplavka.ru)
2. В настройках GitHub Pages укажите Custom domain
3. Обновите `MINI_APP_URL` в `bot.py`
4. Обновите Menu Button в @BotFather

## Поддержка

По всем вопросам:
- Telegram: @yourusername
- GitHub Issues: https://github.com/yourusername/rplavka/issues
